spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/backend-db
    username: backend
    password: Password1
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect